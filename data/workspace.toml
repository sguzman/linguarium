schema_version = 1

interface_language = "en"
metalanguage = "en"
default_semantic_pivot = "en"
default_gloss_language = "en"

# These are operational workspace defaults.
# They do not make English grammar or lexical semantics universal.
